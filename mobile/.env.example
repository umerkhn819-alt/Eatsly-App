# Public API base URL (bundled into the app). No trailing slash.
# iOS simulator / web on same machine:
#   EXPO_PUBLIC_API_URL=http://127.0.0.1:3000
# Android emulator (maps host loopback to the dev machine):
#   EXPO_PUBLIC_API_URL=http://10.0.2.2:3000
EXPO_PUBLIC_API_URL=http://127.0.0.1:3000
